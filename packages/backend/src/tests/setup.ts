// import dotenv from "dotenv";

// // Explicitly load .env.test when running tests
// dotenv.config({ path: ".env.test" });

// console.log("DB Name:", process.env.DB_NAME); // Should print the test DB name
